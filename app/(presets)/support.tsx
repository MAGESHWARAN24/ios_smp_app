import SupportScreen from '@/components/presets/support-screen';
import type { FC } from 'react';

interface SupportProps { }

const Support: FC<SupportProps> = () =>
{
    return (
        <SupportScreen />
    );
}

export default Support;
