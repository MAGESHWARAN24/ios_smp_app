import TransportModeScreen from '@/components/transportmode/screen';
import type { FC } from 'react';

interface TransportModeProps { }

const TransportMode: FC<TransportModeProps> = () =>
{
    return (
        <TransportModeScreen />
    );
}

export default TransportMode;
